"""Hub and daemon scheduler config keys and defaults.

Re-exported from ``repo_scan.config`` where they are now defined (core layer).
This module is kept for backward compatibility — prefer importing from config.
"""

from ..config import HUB_CONFIG_KEYS, HUB_DEFAULTS, cfg_hub  # noqa: F401

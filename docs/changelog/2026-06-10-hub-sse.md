---
type: changelog
date: 2026-06-10
tags:
  - changelog
  - hub
linked_files: ["[[repo_scan/hub/events.py]]"]
---
# Hub SSE live updates

**Date:** 2026-06-10

## What changed

- `/api/events` — `text/event-stream` endpoint with 15s heartbeat comments
- `repo_scan/hub/events.py` — thread-safe fan-out to all SSE subscribers
- Broadcast on `append_event`, `submit_decision`, `create_run`, `update_run`,
  `set_run_stage`
- Client `EventSource` triggers `refresh()` on push; poll slows to 12–30s
  when SSE is connected, reverts to 3–12s adaptive poll on disconnect

## Tests

- `tests/test_hub.py` — event bus, SSE auth, connected frame, client wiring
